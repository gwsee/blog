// Code generated by ent, DO NOT EDIT.

package ent

import (
	"blog/internal/ent/predicate"
	"blog/internal/ent/userexperience"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserExperienceQuery is the builder for querying UserExperience entities.
type UserExperienceQuery struct {
	config
	ctx        *QueryContext
	order      []userexperience.OrderOption
	inters     []Interceptor
	predicates []predicate.UserExperience
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserExperienceQuery builder.
func (ueq *UserExperienceQuery) Where(ps ...predicate.UserExperience) *UserExperienceQuery {
	ueq.predicates = append(ueq.predicates, ps...)
	return ueq
}

// Limit the number of records to be returned by this query.
func (ueq *UserExperienceQuery) Limit(limit int) *UserExperienceQuery {
	ueq.ctx.Limit = &limit
	return ueq
}

// Offset to start from.
func (ueq *UserExperienceQuery) Offset(offset int) *UserExperienceQuery {
	ueq.ctx.Offset = &offset
	return ueq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ueq *UserExperienceQuery) Unique(unique bool) *UserExperienceQuery {
	ueq.ctx.Unique = &unique
	return ueq
}

// Order specifies how the records should be ordered.
func (ueq *UserExperienceQuery) Order(o ...userexperience.OrderOption) *UserExperienceQuery {
	ueq.order = append(ueq.order, o...)
	return ueq
}

// First returns the first UserExperience entity from the query.
// Returns a *NotFoundError when no UserExperience was found.
func (ueq *UserExperienceQuery) First(ctx context.Context) (*UserExperience, error) {
	nodes, err := ueq.Limit(1).All(setContextOp(ctx, ueq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userexperience.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ueq *UserExperienceQuery) FirstX(ctx context.Context) *UserExperience {
	node, err := ueq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserExperience ID from the query.
// Returns a *NotFoundError when no UserExperience ID was found.
func (ueq *UserExperienceQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ueq.Limit(1).IDs(setContextOp(ctx, ueq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userexperience.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ueq *UserExperienceQuery) FirstIDX(ctx context.Context) int {
	id, err := ueq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserExperience entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserExperience entity is found.
// Returns a *NotFoundError when no UserExperience entities are found.
func (ueq *UserExperienceQuery) Only(ctx context.Context) (*UserExperience, error) {
	nodes, err := ueq.Limit(2).All(setContextOp(ctx, ueq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userexperience.Label}
	default:
		return nil, &NotSingularError{userexperience.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ueq *UserExperienceQuery) OnlyX(ctx context.Context) *UserExperience {
	node, err := ueq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserExperience ID in the query.
// Returns a *NotSingularError when more than one UserExperience ID is found.
// Returns a *NotFoundError when no entities are found.
func (ueq *UserExperienceQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ueq.Limit(2).IDs(setContextOp(ctx, ueq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userexperience.Label}
	default:
		err = &NotSingularError{userexperience.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ueq *UserExperienceQuery) OnlyIDX(ctx context.Context) int {
	id, err := ueq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserExperiences.
func (ueq *UserExperienceQuery) All(ctx context.Context) ([]*UserExperience, error) {
	ctx = setContextOp(ctx, ueq.ctx, ent.OpQueryAll)
	if err := ueq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserExperience, *UserExperienceQuery]()
	return withInterceptors[[]*UserExperience](ctx, ueq, qr, ueq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ueq *UserExperienceQuery) AllX(ctx context.Context) []*UserExperience {
	nodes, err := ueq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserExperience IDs.
func (ueq *UserExperienceQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ueq.ctx.Unique == nil && ueq.path != nil {
		ueq.Unique(true)
	}
	ctx = setContextOp(ctx, ueq.ctx, ent.OpQueryIDs)
	if err = ueq.Select(userexperience.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ueq *UserExperienceQuery) IDsX(ctx context.Context) []int {
	ids, err := ueq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ueq *UserExperienceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ueq.ctx, ent.OpQueryCount)
	if err := ueq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ueq, querierCount[*UserExperienceQuery](), ueq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ueq *UserExperienceQuery) CountX(ctx context.Context) int {
	count, err := ueq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ueq *UserExperienceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ueq.ctx, ent.OpQueryExist)
	switch _, err := ueq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ueq *UserExperienceQuery) ExistX(ctx context.Context) bool {
	exist, err := ueq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserExperienceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ueq *UserExperienceQuery) Clone() *UserExperienceQuery {
	if ueq == nil {
		return nil
	}
	return &UserExperienceQuery{
		config:     ueq.config,
		ctx:        ueq.ctx.Clone(),
		order:      append([]userexperience.OrderOption{}, ueq.order...),
		inters:     append([]Interceptor{}, ueq.inters...),
		predicates: append([]predicate.UserExperience{}, ueq.predicates...),
		// clone intermediate query.
		sql:  ueq.sql.Clone(),
		path: ueq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserExperience.Query().
//		GroupBy(userexperience.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ueq *UserExperienceQuery) GroupBy(field string, fields ...string) *UserExperienceGroupBy {
	ueq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserExperienceGroupBy{build: ueq}
	grbuild.flds = &ueq.ctx.Fields
	grbuild.label = userexperience.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//	}
//
//	client.UserExperience.Query().
//		Select(userexperience.FieldCreatedAt).
//		Scan(ctx, &v)
func (ueq *UserExperienceQuery) Select(fields ...string) *UserExperienceSelect {
	ueq.ctx.Fields = append(ueq.ctx.Fields, fields...)
	sbuild := &UserExperienceSelect{UserExperienceQuery: ueq}
	sbuild.label = userexperience.Label
	sbuild.flds, sbuild.scan = &ueq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserExperienceSelect configured with the given aggregations.
func (ueq *UserExperienceQuery) Aggregate(fns ...AggregateFunc) *UserExperienceSelect {
	return ueq.Select().Aggregate(fns...)
}

func (ueq *UserExperienceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ueq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ueq); err != nil {
				return err
			}
		}
	}
	for _, f := range ueq.ctx.Fields {
		if !userexperience.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ueq.path != nil {
		prev, err := ueq.path(ctx)
		if err != nil {
			return err
		}
		ueq.sql = prev
	}
	return nil
}

func (ueq *UserExperienceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserExperience, error) {
	var (
		nodes = []*UserExperience{}
		_spec = ueq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserExperience).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserExperience{config: ueq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ueq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ueq *UserExperienceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ueq.querySpec()
	_spec.Node.Columns = ueq.ctx.Fields
	if len(ueq.ctx.Fields) > 0 {
		_spec.Unique = ueq.ctx.Unique != nil && *ueq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ueq.driver, _spec)
}

func (ueq *UserExperienceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userexperience.Table, userexperience.Columns, sqlgraph.NewFieldSpec(userexperience.FieldID, field.TypeInt))
	_spec.From = ueq.sql
	if unique := ueq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ueq.path != nil {
		_spec.Unique = true
	}
	if fields := ueq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userexperience.FieldID)
		for i := range fields {
			if fields[i] != userexperience.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ueq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ueq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ueq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ueq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ueq *UserExperienceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ueq.driver.Dialect())
	t1 := builder.Table(userexperience.Table)
	columns := ueq.ctx.Fields
	if len(columns) == 0 {
		columns = userexperience.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ueq.sql != nil {
		selector = ueq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ueq.ctx.Unique != nil && *ueq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ueq.predicates {
		p(selector)
	}
	for _, p := range ueq.order {
		p(selector)
	}
	if offset := ueq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ueq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserExperienceGroupBy is the group-by builder for UserExperience entities.
type UserExperienceGroupBy struct {
	selector
	build *UserExperienceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uegb *UserExperienceGroupBy) Aggregate(fns ...AggregateFunc) *UserExperienceGroupBy {
	uegb.fns = append(uegb.fns, fns...)
	return uegb
}

// Scan applies the selector query and scans the result into the given value.
func (uegb *UserExperienceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uegb.build.ctx, ent.OpQueryGroupBy)
	if err := uegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserExperienceQuery, *UserExperienceGroupBy](ctx, uegb.build, uegb, uegb.build.inters, v)
}

func (uegb *UserExperienceGroupBy) sqlScan(ctx context.Context, root *UserExperienceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uegb.fns))
	for _, fn := range uegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uegb.flds)+len(uegb.fns))
		for _, f := range *uegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserExperienceSelect is the builder for selecting fields of UserExperience entities.
type UserExperienceSelect struct {
	*UserExperienceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ues *UserExperienceSelect) Aggregate(fns ...AggregateFunc) *UserExperienceSelect {
	ues.fns = append(ues.fns, fns...)
	return ues
}

// Scan applies the selector query and scans the result into the given value.
func (ues *UserExperienceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ues.ctx, ent.OpQuerySelect)
	if err := ues.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserExperienceQuery, *UserExperienceSelect](ctx, ues.UserExperienceQuery, ues, ues.inters, v)
}

func (ues *UserExperienceSelect) sqlScan(ctx context.Context, root *UserExperienceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ues.fns))
	for _, fn := range ues.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ues.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ues.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
