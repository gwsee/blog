// Code generated by ent, DO NOT EDIT.

package ent

import (
	"blog/internal/ent/palacestodo"
	"blog/internal/ent/palacestododone"
	"blog/internal/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PalacesTodoDoneQuery is the builder for querying PalacesTodoDone entities.
type PalacesTodoDoneQuery struct {
	config
	ctx        *QueryContext
	order      []palacestododone.OrderOption
	inters     []Interceptor
	predicates []predicate.PalacesTodoDone
	withOwner  *PalacesTodoQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PalacesTodoDoneQuery builder.
func (ptdq *PalacesTodoDoneQuery) Where(ps ...predicate.PalacesTodoDone) *PalacesTodoDoneQuery {
	ptdq.predicates = append(ptdq.predicates, ps...)
	return ptdq
}

// Limit the number of records to be returned by this query.
func (ptdq *PalacesTodoDoneQuery) Limit(limit int) *PalacesTodoDoneQuery {
	ptdq.ctx.Limit = &limit
	return ptdq
}

// Offset to start from.
func (ptdq *PalacesTodoDoneQuery) Offset(offset int) *PalacesTodoDoneQuery {
	ptdq.ctx.Offset = &offset
	return ptdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ptdq *PalacesTodoDoneQuery) Unique(unique bool) *PalacesTodoDoneQuery {
	ptdq.ctx.Unique = &unique
	return ptdq
}

// Order specifies how the records should be ordered.
func (ptdq *PalacesTodoDoneQuery) Order(o ...palacestododone.OrderOption) *PalacesTodoDoneQuery {
	ptdq.order = append(ptdq.order, o...)
	return ptdq
}

// QueryOwner chains the current query on the "owner" edge.
func (ptdq *PalacesTodoDoneQuery) QueryOwner() *PalacesTodoQuery {
	query := (&PalacesTodoClient{config: ptdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ptdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ptdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(palacestododone.Table, palacestododone.FieldID, selector),
			sqlgraph.To(palacestodo.Table, palacestodo.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, palacestododone.OwnerTable, palacestododone.OwnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(ptdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PalacesTodoDone entity from the query.
// Returns a *NotFoundError when no PalacesTodoDone was found.
func (ptdq *PalacesTodoDoneQuery) First(ctx context.Context) (*PalacesTodoDone, error) {
	nodes, err := ptdq.Limit(1).All(setContextOp(ctx, ptdq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{palacestododone.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ptdq *PalacesTodoDoneQuery) FirstX(ctx context.Context) *PalacesTodoDone {
	node, err := ptdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PalacesTodoDone ID from the query.
// Returns a *NotFoundError when no PalacesTodoDone ID was found.
func (ptdq *PalacesTodoDoneQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ptdq.Limit(1).IDs(setContextOp(ctx, ptdq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{palacestododone.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ptdq *PalacesTodoDoneQuery) FirstIDX(ctx context.Context) int {
	id, err := ptdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PalacesTodoDone entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PalacesTodoDone entity is found.
// Returns a *NotFoundError when no PalacesTodoDone entities are found.
func (ptdq *PalacesTodoDoneQuery) Only(ctx context.Context) (*PalacesTodoDone, error) {
	nodes, err := ptdq.Limit(2).All(setContextOp(ctx, ptdq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{palacestododone.Label}
	default:
		return nil, &NotSingularError{palacestododone.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ptdq *PalacesTodoDoneQuery) OnlyX(ctx context.Context) *PalacesTodoDone {
	node, err := ptdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PalacesTodoDone ID in the query.
// Returns a *NotSingularError when more than one PalacesTodoDone ID is found.
// Returns a *NotFoundError when no entities are found.
func (ptdq *PalacesTodoDoneQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ptdq.Limit(2).IDs(setContextOp(ctx, ptdq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{palacestododone.Label}
	default:
		err = &NotSingularError{palacestododone.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ptdq *PalacesTodoDoneQuery) OnlyIDX(ctx context.Context) int {
	id, err := ptdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PalacesTodoDones.
func (ptdq *PalacesTodoDoneQuery) All(ctx context.Context) ([]*PalacesTodoDone, error) {
	ctx = setContextOp(ctx, ptdq.ctx, ent.OpQueryAll)
	if err := ptdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PalacesTodoDone, *PalacesTodoDoneQuery]()
	return withInterceptors[[]*PalacesTodoDone](ctx, ptdq, qr, ptdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ptdq *PalacesTodoDoneQuery) AllX(ctx context.Context) []*PalacesTodoDone {
	nodes, err := ptdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PalacesTodoDone IDs.
func (ptdq *PalacesTodoDoneQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ptdq.ctx.Unique == nil && ptdq.path != nil {
		ptdq.Unique(true)
	}
	ctx = setContextOp(ctx, ptdq.ctx, ent.OpQueryIDs)
	if err = ptdq.Select(palacestododone.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ptdq *PalacesTodoDoneQuery) IDsX(ctx context.Context) []int {
	ids, err := ptdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ptdq *PalacesTodoDoneQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ptdq.ctx, ent.OpQueryCount)
	if err := ptdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ptdq, querierCount[*PalacesTodoDoneQuery](), ptdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ptdq *PalacesTodoDoneQuery) CountX(ctx context.Context) int {
	count, err := ptdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ptdq *PalacesTodoDoneQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ptdq.ctx, ent.OpQueryExist)
	switch _, err := ptdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ptdq *PalacesTodoDoneQuery) ExistX(ctx context.Context) bool {
	exist, err := ptdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PalacesTodoDoneQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ptdq *PalacesTodoDoneQuery) Clone() *PalacesTodoDoneQuery {
	if ptdq == nil {
		return nil
	}
	return &PalacesTodoDoneQuery{
		config:     ptdq.config,
		ctx:        ptdq.ctx.Clone(),
		order:      append([]palacestododone.OrderOption{}, ptdq.order...),
		inters:     append([]Interceptor{}, ptdq.inters...),
		predicates: append([]predicate.PalacesTodoDone{}, ptdq.predicates...),
		withOwner:  ptdq.withOwner.Clone(),
		// clone intermediate query.
		sql:  ptdq.sql.Clone(),
		path: ptdq.path,
	}
}

// WithOwner tells the query-builder to eager-load the nodes that are connected to
// the "owner" edge. The optional arguments are used to configure the query builder of the edge.
func (ptdq *PalacesTodoDoneQuery) WithOwner(opts ...func(*PalacesTodoQuery)) *PalacesTodoDoneQuery {
	query := (&PalacesTodoClient{config: ptdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ptdq.withOwner = query
	return ptdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PalacesTodoDone.Query().
//		GroupBy(palacestododone.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ptdq *PalacesTodoDoneQuery) GroupBy(field string, fields ...string) *PalacesTodoDoneGroupBy {
	ptdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PalacesTodoDoneGroupBy{build: ptdq}
	grbuild.flds = &ptdq.ctx.Fields
	grbuild.label = palacestododone.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//	}
//
//	client.PalacesTodoDone.Query().
//		Select(palacestododone.FieldCreatedAt).
//		Scan(ctx, &v)
func (ptdq *PalacesTodoDoneQuery) Select(fields ...string) *PalacesTodoDoneSelect {
	ptdq.ctx.Fields = append(ptdq.ctx.Fields, fields...)
	sbuild := &PalacesTodoDoneSelect{PalacesTodoDoneQuery: ptdq}
	sbuild.label = palacestododone.Label
	sbuild.flds, sbuild.scan = &ptdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PalacesTodoDoneSelect configured with the given aggregations.
func (ptdq *PalacesTodoDoneQuery) Aggregate(fns ...AggregateFunc) *PalacesTodoDoneSelect {
	return ptdq.Select().Aggregate(fns...)
}

func (ptdq *PalacesTodoDoneQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ptdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ptdq); err != nil {
				return err
			}
		}
	}
	for _, f := range ptdq.ctx.Fields {
		if !palacestododone.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ptdq.path != nil {
		prev, err := ptdq.path(ctx)
		if err != nil {
			return err
		}
		ptdq.sql = prev
	}
	return nil
}

func (ptdq *PalacesTodoDoneQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PalacesTodoDone, error) {
	var (
		nodes       = []*PalacesTodoDone{}
		_spec       = ptdq.querySpec()
		loadedTypes = [1]bool{
			ptdq.withOwner != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PalacesTodoDone).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PalacesTodoDone{config: ptdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ptdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ptdq.withOwner; query != nil {
		if err := ptdq.loadOwner(ctx, query, nodes, nil,
			func(n *PalacesTodoDone, e *PalacesTodo) { n.Edges.Owner = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ptdq *PalacesTodoDoneQuery) loadOwner(ctx context.Context, query *PalacesTodoQuery, nodes []*PalacesTodoDone, init func(*PalacesTodoDone), assign func(*PalacesTodoDone, *PalacesTodo)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PalacesTodoDone)
	for i := range nodes {
		fk := nodes[i].TodoID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(palacestodo.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "todo_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ptdq *PalacesTodoDoneQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ptdq.querySpec()
	_spec.Node.Columns = ptdq.ctx.Fields
	if len(ptdq.ctx.Fields) > 0 {
		_spec.Unique = ptdq.ctx.Unique != nil && *ptdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ptdq.driver, _spec)
}

func (ptdq *PalacesTodoDoneQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(palacestododone.Table, palacestododone.Columns, sqlgraph.NewFieldSpec(palacestododone.FieldID, field.TypeInt))
	_spec.From = ptdq.sql
	if unique := ptdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ptdq.path != nil {
		_spec.Unique = true
	}
	if fields := ptdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, palacestododone.FieldID)
		for i := range fields {
			if fields[i] != palacestododone.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ptdq.withOwner != nil {
			_spec.Node.AddColumnOnce(palacestododone.FieldTodoID)
		}
	}
	if ps := ptdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ptdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ptdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ptdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ptdq *PalacesTodoDoneQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ptdq.driver.Dialect())
	t1 := builder.Table(palacestododone.Table)
	columns := ptdq.ctx.Fields
	if len(columns) == 0 {
		columns = palacestododone.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ptdq.sql != nil {
		selector = ptdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ptdq.ctx.Unique != nil && *ptdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ptdq.predicates {
		p(selector)
	}
	for _, p := range ptdq.order {
		p(selector)
	}
	if offset := ptdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ptdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PalacesTodoDoneGroupBy is the group-by builder for PalacesTodoDone entities.
type PalacesTodoDoneGroupBy struct {
	selector
	build *PalacesTodoDoneQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ptdgb *PalacesTodoDoneGroupBy) Aggregate(fns ...AggregateFunc) *PalacesTodoDoneGroupBy {
	ptdgb.fns = append(ptdgb.fns, fns...)
	return ptdgb
}

// Scan applies the selector query and scans the result into the given value.
func (ptdgb *PalacesTodoDoneGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ptdgb.build.ctx, ent.OpQueryGroupBy)
	if err := ptdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PalacesTodoDoneQuery, *PalacesTodoDoneGroupBy](ctx, ptdgb.build, ptdgb, ptdgb.build.inters, v)
}

func (ptdgb *PalacesTodoDoneGroupBy) sqlScan(ctx context.Context, root *PalacesTodoDoneQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ptdgb.fns))
	for _, fn := range ptdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ptdgb.flds)+len(ptdgb.fns))
		for _, f := range *ptdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ptdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ptdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PalacesTodoDoneSelect is the builder for selecting fields of PalacesTodoDone entities.
type PalacesTodoDoneSelect struct {
	*PalacesTodoDoneQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ptds *PalacesTodoDoneSelect) Aggregate(fns ...AggregateFunc) *PalacesTodoDoneSelect {
	ptds.fns = append(ptds.fns, fns...)
	return ptds
}

// Scan applies the selector query and scans the result into the given value.
func (ptds *PalacesTodoDoneSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ptds.ctx, ent.OpQuerySelect)
	if err := ptds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PalacesTodoDoneQuery, *PalacesTodoDoneSelect](ctx, ptds.PalacesTodoDoneQuery, ptds, ptds.inters, v)
}

func (ptds *PalacesTodoDoneSelect) sqlScan(ctx context.Context, root *PalacesTodoDoneQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ptds.fns))
	for _, fn := range ptds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ptds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ptds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
