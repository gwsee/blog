// Code generated by ent, DO NOT EDIT.

package ent

import (
	"blog/internal/ent/predicate"
	"blog/internal/ent/userfamousquotes"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserFamousQuotesQuery is the builder for querying UserFamousQuotes entities.
type UserFamousQuotesQuery struct {
	config
	ctx        *QueryContext
	order      []userfamousquotes.OrderOption
	inters     []Interceptor
	predicates []predicate.UserFamousQuotes
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserFamousQuotesQuery builder.
func (ufqq *UserFamousQuotesQuery) Where(ps ...predicate.UserFamousQuotes) *UserFamousQuotesQuery {
	ufqq.predicates = append(ufqq.predicates, ps...)
	return ufqq
}

// Limit the number of records to be returned by this query.
func (ufqq *UserFamousQuotesQuery) Limit(limit int) *UserFamousQuotesQuery {
	ufqq.ctx.Limit = &limit
	return ufqq
}

// Offset to start from.
func (ufqq *UserFamousQuotesQuery) Offset(offset int) *UserFamousQuotesQuery {
	ufqq.ctx.Offset = &offset
	return ufqq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ufqq *UserFamousQuotesQuery) Unique(unique bool) *UserFamousQuotesQuery {
	ufqq.ctx.Unique = &unique
	return ufqq
}

// Order specifies how the records should be ordered.
func (ufqq *UserFamousQuotesQuery) Order(o ...userfamousquotes.OrderOption) *UserFamousQuotesQuery {
	ufqq.order = append(ufqq.order, o...)
	return ufqq
}

// First returns the first UserFamousQuotes entity from the query.
// Returns a *NotFoundError when no UserFamousQuotes was found.
func (ufqq *UserFamousQuotesQuery) First(ctx context.Context) (*UserFamousQuotes, error) {
	nodes, err := ufqq.Limit(1).All(setContextOp(ctx, ufqq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userfamousquotes.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ufqq *UserFamousQuotesQuery) FirstX(ctx context.Context) *UserFamousQuotes {
	node, err := ufqq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserFamousQuotes ID from the query.
// Returns a *NotFoundError when no UserFamousQuotes ID was found.
func (ufqq *UserFamousQuotesQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ufqq.Limit(1).IDs(setContextOp(ctx, ufqq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userfamousquotes.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ufqq *UserFamousQuotesQuery) FirstIDX(ctx context.Context) int {
	id, err := ufqq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserFamousQuotes entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserFamousQuotes entity is found.
// Returns a *NotFoundError when no UserFamousQuotes entities are found.
func (ufqq *UserFamousQuotesQuery) Only(ctx context.Context) (*UserFamousQuotes, error) {
	nodes, err := ufqq.Limit(2).All(setContextOp(ctx, ufqq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userfamousquotes.Label}
	default:
		return nil, &NotSingularError{userfamousquotes.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ufqq *UserFamousQuotesQuery) OnlyX(ctx context.Context) *UserFamousQuotes {
	node, err := ufqq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserFamousQuotes ID in the query.
// Returns a *NotSingularError when more than one UserFamousQuotes ID is found.
// Returns a *NotFoundError when no entities are found.
func (ufqq *UserFamousQuotesQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ufqq.Limit(2).IDs(setContextOp(ctx, ufqq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userfamousquotes.Label}
	default:
		err = &NotSingularError{userfamousquotes.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ufqq *UserFamousQuotesQuery) OnlyIDX(ctx context.Context) int {
	id, err := ufqq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserFamousQuotesSlice.
func (ufqq *UserFamousQuotesQuery) All(ctx context.Context) ([]*UserFamousQuotes, error) {
	ctx = setContextOp(ctx, ufqq.ctx, ent.OpQueryAll)
	if err := ufqq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserFamousQuotes, *UserFamousQuotesQuery]()
	return withInterceptors[[]*UserFamousQuotes](ctx, ufqq, qr, ufqq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ufqq *UserFamousQuotesQuery) AllX(ctx context.Context) []*UserFamousQuotes {
	nodes, err := ufqq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserFamousQuotes IDs.
func (ufqq *UserFamousQuotesQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ufqq.ctx.Unique == nil && ufqq.path != nil {
		ufqq.Unique(true)
	}
	ctx = setContextOp(ctx, ufqq.ctx, ent.OpQueryIDs)
	if err = ufqq.Select(userfamousquotes.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ufqq *UserFamousQuotesQuery) IDsX(ctx context.Context) []int {
	ids, err := ufqq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ufqq *UserFamousQuotesQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ufqq.ctx, ent.OpQueryCount)
	if err := ufqq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ufqq, querierCount[*UserFamousQuotesQuery](), ufqq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ufqq *UserFamousQuotesQuery) CountX(ctx context.Context) int {
	count, err := ufqq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ufqq *UserFamousQuotesQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ufqq.ctx, ent.OpQueryExist)
	switch _, err := ufqq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ufqq *UserFamousQuotesQuery) ExistX(ctx context.Context) bool {
	exist, err := ufqq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserFamousQuotesQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ufqq *UserFamousQuotesQuery) Clone() *UserFamousQuotesQuery {
	if ufqq == nil {
		return nil
	}
	return &UserFamousQuotesQuery{
		config:     ufqq.config,
		ctx:        ufqq.ctx.Clone(),
		order:      append([]userfamousquotes.OrderOption{}, ufqq.order...),
		inters:     append([]Interceptor{}, ufqq.inters...),
		predicates: append([]predicate.UserFamousQuotes{}, ufqq.predicates...),
		// clone intermediate query.
		sql:  ufqq.sql.Clone(),
		path: ufqq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserFamousQuotes.Query().
//		GroupBy(userfamousquotes.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ufqq *UserFamousQuotesQuery) GroupBy(field string, fields ...string) *UserFamousQuotesGroupBy {
	ufqq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserFamousQuotesGroupBy{build: ufqq}
	grbuild.flds = &ufqq.ctx.Fields
	grbuild.label = userfamousquotes.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//	}
//
//	client.UserFamousQuotes.Query().
//		Select(userfamousquotes.FieldCreatedAt).
//		Scan(ctx, &v)
func (ufqq *UserFamousQuotesQuery) Select(fields ...string) *UserFamousQuotesSelect {
	ufqq.ctx.Fields = append(ufqq.ctx.Fields, fields...)
	sbuild := &UserFamousQuotesSelect{UserFamousQuotesQuery: ufqq}
	sbuild.label = userfamousquotes.Label
	sbuild.flds, sbuild.scan = &ufqq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserFamousQuotesSelect configured with the given aggregations.
func (ufqq *UserFamousQuotesQuery) Aggregate(fns ...AggregateFunc) *UserFamousQuotesSelect {
	return ufqq.Select().Aggregate(fns...)
}

func (ufqq *UserFamousQuotesQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ufqq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ufqq); err != nil {
				return err
			}
		}
	}
	for _, f := range ufqq.ctx.Fields {
		if !userfamousquotes.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ufqq.path != nil {
		prev, err := ufqq.path(ctx)
		if err != nil {
			return err
		}
		ufqq.sql = prev
	}
	return nil
}

func (ufqq *UserFamousQuotesQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserFamousQuotes, error) {
	var (
		nodes = []*UserFamousQuotes{}
		_spec = ufqq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserFamousQuotes).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserFamousQuotes{config: ufqq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ufqq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ufqq *UserFamousQuotesQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ufqq.querySpec()
	_spec.Node.Columns = ufqq.ctx.Fields
	if len(ufqq.ctx.Fields) > 0 {
		_spec.Unique = ufqq.ctx.Unique != nil && *ufqq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ufqq.driver, _spec)
}

func (ufqq *UserFamousQuotesQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userfamousquotes.Table, userfamousquotes.Columns, sqlgraph.NewFieldSpec(userfamousquotes.FieldID, field.TypeInt))
	_spec.From = ufqq.sql
	if unique := ufqq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ufqq.path != nil {
		_spec.Unique = true
	}
	if fields := ufqq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userfamousquotes.FieldID)
		for i := range fields {
			if fields[i] != userfamousquotes.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ufqq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ufqq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ufqq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ufqq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ufqq *UserFamousQuotesQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ufqq.driver.Dialect())
	t1 := builder.Table(userfamousquotes.Table)
	columns := ufqq.ctx.Fields
	if len(columns) == 0 {
		columns = userfamousquotes.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ufqq.sql != nil {
		selector = ufqq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ufqq.ctx.Unique != nil && *ufqq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ufqq.predicates {
		p(selector)
	}
	for _, p := range ufqq.order {
		p(selector)
	}
	if offset := ufqq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ufqq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserFamousQuotesGroupBy is the group-by builder for UserFamousQuotes entities.
type UserFamousQuotesGroupBy struct {
	selector
	build *UserFamousQuotesQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ufqgb *UserFamousQuotesGroupBy) Aggregate(fns ...AggregateFunc) *UserFamousQuotesGroupBy {
	ufqgb.fns = append(ufqgb.fns, fns...)
	return ufqgb
}

// Scan applies the selector query and scans the result into the given value.
func (ufqgb *UserFamousQuotesGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ufqgb.build.ctx, ent.OpQueryGroupBy)
	if err := ufqgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserFamousQuotesQuery, *UserFamousQuotesGroupBy](ctx, ufqgb.build, ufqgb, ufqgb.build.inters, v)
}

func (ufqgb *UserFamousQuotesGroupBy) sqlScan(ctx context.Context, root *UserFamousQuotesQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ufqgb.fns))
	for _, fn := range ufqgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ufqgb.flds)+len(ufqgb.fns))
		for _, f := range *ufqgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ufqgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ufqgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserFamousQuotesSelect is the builder for selecting fields of UserFamousQuotes entities.
type UserFamousQuotesSelect struct {
	*UserFamousQuotesQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ufqs *UserFamousQuotesSelect) Aggregate(fns ...AggregateFunc) *UserFamousQuotesSelect {
	ufqs.fns = append(ufqs.fns, fns...)
	return ufqs
}

// Scan applies the selector query and scans the result into the given value.
func (ufqs *UserFamousQuotesSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ufqs.ctx, ent.OpQuerySelect)
	if err := ufqs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserFamousQuotesQuery, *UserFamousQuotesSelect](ctx, ufqs.UserFamousQuotesQuery, ufqs, ufqs.inters, v)
}

func (ufqs *UserFamousQuotesSelect) sqlScan(ctx context.Context, root *UserFamousQuotesQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ufqs.fns))
	for _, fn := range ufqs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ufqs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ufqs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
