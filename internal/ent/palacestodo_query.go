// Code generated by ent, DO NOT EDIT.

package ent

import (
	"blog/internal/ent/palacestodo"
	"blog/internal/ent/palacestododone"
	"blog/internal/ent/predicate"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PalacesTodoQuery is the builder for querying PalacesTodo entities.
type PalacesTodoQuery struct {
	config
	ctx        *QueryContext
	order      []palacestodo.OrderOption
	inters     []Interceptor
	predicates []predicate.PalacesTodo
	withDones  *PalacesTodoDoneQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PalacesTodoQuery builder.
func (ptq *PalacesTodoQuery) Where(ps ...predicate.PalacesTodo) *PalacesTodoQuery {
	ptq.predicates = append(ptq.predicates, ps...)
	return ptq
}

// Limit the number of records to be returned by this query.
func (ptq *PalacesTodoQuery) Limit(limit int) *PalacesTodoQuery {
	ptq.ctx.Limit = &limit
	return ptq
}

// Offset to start from.
func (ptq *PalacesTodoQuery) Offset(offset int) *PalacesTodoQuery {
	ptq.ctx.Offset = &offset
	return ptq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ptq *PalacesTodoQuery) Unique(unique bool) *PalacesTodoQuery {
	ptq.ctx.Unique = &unique
	return ptq
}

// Order specifies how the records should be ordered.
func (ptq *PalacesTodoQuery) Order(o ...palacestodo.OrderOption) *PalacesTodoQuery {
	ptq.order = append(ptq.order, o...)
	return ptq
}

// QueryDones chains the current query on the "dones" edge.
func (ptq *PalacesTodoQuery) QueryDones() *PalacesTodoDoneQuery {
	query := (&PalacesTodoDoneClient{config: ptq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ptq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ptq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(palacestodo.Table, palacestodo.FieldID, selector),
			sqlgraph.To(palacestododone.Table, palacestododone.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, palacestodo.DonesTable, palacestodo.DonesColumn),
		)
		fromU = sqlgraph.SetNeighbors(ptq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PalacesTodo entity from the query.
// Returns a *NotFoundError when no PalacesTodo was found.
func (ptq *PalacesTodoQuery) First(ctx context.Context) (*PalacesTodo, error) {
	nodes, err := ptq.Limit(1).All(setContextOp(ctx, ptq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{palacestodo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ptq *PalacesTodoQuery) FirstX(ctx context.Context) *PalacesTodo {
	node, err := ptq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PalacesTodo ID from the query.
// Returns a *NotFoundError when no PalacesTodo ID was found.
func (ptq *PalacesTodoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ptq.Limit(1).IDs(setContextOp(ctx, ptq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{palacestodo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ptq *PalacesTodoQuery) FirstIDX(ctx context.Context) int {
	id, err := ptq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PalacesTodo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PalacesTodo entity is found.
// Returns a *NotFoundError when no PalacesTodo entities are found.
func (ptq *PalacesTodoQuery) Only(ctx context.Context) (*PalacesTodo, error) {
	nodes, err := ptq.Limit(2).All(setContextOp(ctx, ptq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{palacestodo.Label}
	default:
		return nil, &NotSingularError{palacestodo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ptq *PalacesTodoQuery) OnlyX(ctx context.Context) *PalacesTodo {
	node, err := ptq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PalacesTodo ID in the query.
// Returns a *NotSingularError when more than one PalacesTodo ID is found.
// Returns a *NotFoundError when no entities are found.
func (ptq *PalacesTodoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ptq.Limit(2).IDs(setContextOp(ctx, ptq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{palacestodo.Label}
	default:
		err = &NotSingularError{palacestodo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ptq *PalacesTodoQuery) OnlyIDX(ctx context.Context) int {
	id, err := ptq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PalacesTodos.
func (ptq *PalacesTodoQuery) All(ctx context.Context) ([]*PalacesTodo, error) {
	ctx = setContextOp(ctx, ptq.ctx, ent.OpQueryAll)
	if err := ptq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PalacesTodo, *PalacesTodoQuery]()
	return withInterceptors[[]*PalacesTodo](ctx, ptq, qr, ptq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ptq *PalacesTodoQuery) AllX(ctx context.Context) []*PalacesTodo {
	nodes, err := ptq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PalacesTodo IDs.
func (ptq *PalacesTodoQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ptq.ctx.Unique == nil && ptq.path != nil {
		ptq.Unique(true)
	}
	ctx = setContextOp(ctx, ptq.ctx, ent.OpQueryIDs)
	if err = ptq.Select(palacestodo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ptq *PalacesTodoQuery) IDsX(ctx context.Context) []int {
	ids, err := ptq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ptq *PalacesTodoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ptq.ctx, ent.OpQueryCount)
	if err := ptq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ptq, querierCount[*PalacesTodoQuery](), ptq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ptq *PalacesTodoQuery) CountX(ctx context.Context) int {
	count, err := ptq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ptq *PalacesTodoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ptq.ctx, ent.OpQueryExist)
	switch _, err := ptq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ptq *PalacesTodoQuery) ExistX(ctx context.Context) bool {
	exist, err := ptq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PalacesTodoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ptq *PalacesTodoQuery) Clone() *PalacesTodoQuery {
	if ptq == nil {
		return nil
	}
	return &PalacesTodoQuery{
		config:     ptq.config,
		ctx:        ptq.ctx.Clone(),
		order:      append([]palacestodo.OrderOption{}, ptq.order...),
		inters:     append([]Interceptor{}, ptq.inters...),
		predicates: append([]predicate.PalacesTodo{}, ptq.predicates...),
		withDones:  ptq.withDones.Clone(),
		// clone intermediate query.
		sql:  ptq.sql.Clone(),
		path: ptq.path,
	}
}

// WithDones tells the query-builder to eager-load the nodes that are connected to
// the "dones" edge. The optional arguments are used to configure the query builder of the edge.
func (ptq *PalacesTodoQuery) WithDones(opts ...func(*PalacesTodoDoneQuery)) *PalacesTodoQuery {
	query := (&PalacesTodoDoneClient{config: ptq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ptq.withDones = query
	return ptq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PalacesTodo.Query().
//		GroupBy(palacestodo.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ptq *PalacesTodoQuery) GroupBy(field string, fields ...string) *PalacesTodoGroupBy {
	ptq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PalacesTodoGroupBy{build: ptq}
	grbuild.flds = &ptq.ctx.Fields
	grbuild.label = palacestodo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//	}
//
//	client.PalacesTodo.Query().
//		Select(palacestodo.FieldCreatedAt).
//		Scan(ctx, &v)
func (ptq *PalacesTodoQuery) Select(fields ...string) *PalacesTodoSelect {
	ptq.ctx.Fields = append(ptq.ctx.Fields, fields...)
	sbuild := &PalacesTodoSelect{PalacesTodoQuery: ptq}
	sbuild.label = palacestodo.Label
	sbuild.flds, sbuild.scan = &ptq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PalacesTodoSelect configured with the given aggregations.
func (ptq *PalacesTodoQuery) Aggregate(fns ...AggregateFunc) *PalacesTodoSelect {
	return ptq.Select().Aggregate(fns...)
}

func (ptq *PalacesTodoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ptq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ptq); err != nil {
				return err
			}
		}
	}
	for _, f := range ptq.ctx.Fields {
		if !palacestodo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ptq.path != nil {
		prev, err := ptq.path(ctx)
		if err != nil {
			return err
		}
		ptq.sql = prev
	}
	return nil
}

func (ptq *PalacesTodoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PalacesTodo, error) {
	var (
		nodes       = []*PalacesTodo{}
		_spec       = ptq.querySpec()
		loadedTypes = [1]bool{
			ptq.withDones != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PalacesTodo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PalacesTodo{config: ptq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ptq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ptq.withDones; query != nil {
		if err := ptq.loadDones(ctx, query, nodes,
			func(n *PalacesTodo) { n.Edges.Dones = []*PalacesTodoDone{} },
			func(n *PalacesTodo, e *PalacesTodoDone) { n.Edges.Dones = append(n.Edges.Dones, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ptq *PalacesTodoQuery) loadDones(ctx context.Context, query *PalacesTodoDoneQuery, nodes []*PalacesTodo, init func(*PalacesTodo), assign func(*PalacesTodo, *PalacesTodoDone)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*PalacesTodo)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(palacestododone.FieldTodoID)
	}
	query.Where(predicate.PalacesTodoDone(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(palacestodo.DonesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.TodoID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "todo_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (ptq *PalacesTodoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ptq.querySpec()
	_spec.Node.Columns = ptq.ctx.Fields
	if len(ptq.ctx.Fields) > 0 {
		_spec.Unique = ptq.ctx.Unique != nil && *ptq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ptq.driver, _spec)
}

func (ptq *PalacesTodoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(palacestodo.Table, palacestodo.Columns, sqlgraph.NewFieldSpec(palacestodo.FieldID, field.TypeInt))
	_spec.From = ptq.sql
	if unique := ptq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ptq.path != nil {
		_spec.Unique = true
	}
	if fields := ptq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, palacestodo.FieldID)
		for i := range fields {
			if fields[i] != palacestodo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ptq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ptq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ptq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ptq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ptq *PalacesTodoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ptq.driver.Dialect())
	t1 := builder.Table(palacestodo.Table)
	columns := ptq.ctx.Fields
	if len(columns) == 0 {
		columns = palacestodo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ptq.sql != nil {
		selector = ptq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ptq.ctx.Unique != nil && *ptq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ptq.predicates {
		p(selector)
	}
	for _, p := range ptq.order {
		p(selector)
	}
	if offset := ptq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ptq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PalacesTodoGroupBy is the group-by builder for PalacesTodo entities.
type PalacesTodoGroupBy struct {
	selector
	build *PalacesTodoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ptgb *PalacesTodoGroupBy) Aggregate(fns ...AggregateFunc) *PalacesTodoGroupBy {
	ptgb.fns = append(ptgb.fns, fns...)
	return ptgb
}

// Scan applies the selector query and scans the result into the given value.
func (ptgb *PalacesTodoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ptgb.build.ctx, ent.OpQueryGroupBy)
	if err := ptgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PalacesTodoQuery, *PalacesTodoGroupBy](ctx, ptgb.build, ptgb, ptgb.build.inters, v)
}

func (ptgb *PalacesTodoGroupBy) sqlScan(ctx context.Context, root *PalacesTodoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ptgb.fns))
	for _, fn := range ptgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ptgb.flds)+len(ptgb.fns))
		for _, f := range *ptgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ptgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ptgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PalacesTodoSelect is the builder for selecting fields of PalacesTodo entities.
type PalacesTodoSelect struct {
	*PalacesTodoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pts *PalacesTodoSelect) Aggregate(fns ...AggregateFunc) *PalacesTodoSelect {
	pts.fns = append(pts.fns, fns...)
	return pts
}

// Scan applies the selector query and scans the result into the given value.
func (pts *PalacesTodoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pts.ctx, ent.OpQuerySelect)
	if err := pts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PalacesTodoQuery, *PalacesTodoSelect](ctx, pts.PalacesTodoQuery, pts, pts.inters, v)
}

func (pts *PalacesTodoSelect) sqlScan(ctx context.Context, root *PalacesTodoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pts.fns))
	for _, fn := range pts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
